export async function onRequestGet({ request, env }) { const url = new URL(request.url); const github = new URL('https://github.com/login/oauth/authorize'); github.searchParams.set('client_id', env.GITHUB_CLIENT_ID); github.searchParams.set('redirect_uri', `${url.origin}/api/callback`); github.searchParams.set('scope', 'repo,user'); return Response.redirect(github.toString(), 302); }
