export async function onRequestGet({ request, env }) { const url = new URL(request.url); const code = url.searchParams.get('code'); if (!code) return new Response('Missing GitHub authorization code.', {status:400}); const response = await fetch('https://github.com/login/oauth/access_token',{method:'POST',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify({client_id:env.GITHUB_CLIENT_ID,client_secret:env.GITHUB_CLIENT_SECRET,code})}); const data = await response.json(); if (!data.access_token) return new Response('GitHub authorization failed.', {status:401}); const payload = JSON.stringify({token:data.access_token,provider:'github'}).replace(/</g,'\\u003c'); return new Response(`<!doctype html><script>window.opener.postMessage('authorization:github:success:' + '${payload}', window.location.origin);window.close();</script>`,{headers:{'Content-Type':'text/html; charset=UTF-8'}}); }
