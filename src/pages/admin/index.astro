---
import BaseLayout from '../../layouts/BaseLayout.astro';
---
<BaseLayout title="laconicleon 编辑后台">
  <a class="project-manager" href="/admin/projects/">项目管理 ↗</a>
  <div id="nc-root"><div class="loading" id="cms-loading">正在加载编辑后台…</div></div>
  <script is:inline>window.CMS_MANUAL_INIT = true;</script>
  <script is:inline defer src="https://unpkg.com/decap-cms@3.15.1/dist/decap-cms.js"></script>
  <script is:inline>
    window.addEventListener('DOMContentLoaded', () => {
      const loading = document.getElementById('cms-loading');
      if (typeof window.initCMS === 'function') {
        window.initCMS();
      } else if (loading) {
        loading.textContent = '编辑后台加载失败，请刷新页面后重试。';
      }
    });
  </script>
</BaseLayout>
<style>.project-manager{position:fixed;z-index:9999;right:18px;bottom:18px;padding:11px 15px;background:#b3251d;color:#fff;font:12px var(--mono);box-shadow:0 4px 16px #0003}.loading{min-height:100vh;display:grid;place-items:center;font:18px var(--serif);color:#555}</style>
