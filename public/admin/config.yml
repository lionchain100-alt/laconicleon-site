# GitHub OAuth 凭据配置完成后，后台可在 /admin 使用。
backend:
  name: github
  repo: lionchain100-alt/laconicleon-site
  branch: main
  base_url: https://laconicleon.com
  auth_endpoint: api/auth
media_folder: public/uploads
public_folder: /uploads
collections:
  - name: posts
    label: 文章
    folder: src/content/posts
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    summary: "{{publishedAt | date('YYYY-MM-DD HH:mm')}} · {{title}}"
    sortable_fields: [publishedAt, commit_date, title, language, draft]
    sort: "publishedAt:desc"
    view_filters:
      - { label: "草稿", field: draft, pattern: true }
      - { label: "已发布", field: draft, pattern: false }
      - { label: "中文", field: language, pattern: zh }
      - { label: "English", field: language, pattern: en }
    fields:
      - { label: 标题, name: title, widget: string }
      - { label: 摘要, name: description, widget: text }
      - { label: 发布日期, name: publishedAt, widget: datetime, format: "YYYY-MM-DDTHH:mm:ssZ", hint: "精确到分钟；后台与网站均按此时间从新到旧排列。" }
      - { label: 草稿, name: draft, widget: boolean, default: false, required: false }
      - { label: 首页精选, name: featured, widget: boolean, default: false, required: false }
      - { label: 分类, name: category, widget: string, default: "写作", required: false }
      - { label: 展示主题, name: theme, widget: select, options: ["writing", "tools", "systems", "notes"], default: "notes", hint: "文章页的一级筛选；写作、工具、系统、随笔分别对应 writing、tools、systems、notes。" }
      - { label: 封面图, name: cover, widget: image, required: false }
      - { label: 标签, name: tags, widget: list, required: false }
      - { label: 语言, name: language, widget: select, options: ["zh", "en"], default: "zh" }
      - { label: 翻译编号, name: translationKey, widget: string, required: false, hint: "中英文版本使用同一编号，例如 png-to-pdf" }
      - { label: 正文, name: body, widget: markdown, hint: "页面会自动显示上方标题；正文请从引言或二级标题开始，不要再写一级标题。" }
